import{_ as e,c as i,a2 as s,o as t}from"./chunks/framework.DXKzXLg0.js";const g=JSON.parse('{"title":"Installing plugins","description":"","frontmatter":{},"headers":[],"relativePath":"101/installing-plugins.md","filePath":"101/installing-plugins.md"}'),n={name:"101/installing-plugins.md"};function o(p,a,l,c,h,d){return t(),i("div",null,a[0]||(a[0]=[s(`<h1 id="installing-plugins" tabindex="-1">Installing plugins <a class="header-anchor" href="#installing-plugins" aria-label="Permalink to &quot;Installing plugins&quot;">​</a></h1><p>Here I want to show you how to install a plugin without a plugin manager.</p><p>Vim and Neovim have a feature called <a href="https://neovim.io/doc/user/repeat.html#_using-vim-packages" target="_blank" rel="noreferrer">packages</a>, and this is enough to install many plugins that are hosted on github.</p><h2 id="be-aware" tabindex="-1">Be aware <a class="header-anchor" href="#be-aware" aria-label="Permalink to &quot;Be aware&quot;">​</a></h2><p>Notice the title says <strong>install</strong>. That&#39;s the only thing a Vim package can do. Is just a way to load a plugin into Neovim. Right now there isn&#39;t a mechanism to update or delete a plugin.</p><p>If you are the kind of person that plans to download 30 plugins and try to keep them up to date, the approach I&#39;m going to show is not going to be good enough for you.</p><h2 id="the-pack-directory" tabindex="-1">The pack directory <a class="header-anchor" href="#the-pack-directory" aria-label="Permalink to &quot;The pack directory&quot;">​</a></h2><p>There is an option called <a href="https://neovim.io/doc/user/options.html#&#39;packpath&#39;" target="_blank" rel="noreferrer">packpath</a>. This is a list of directories where we can add a <code>pack</code> directory.</p><p>Inside the <code>pack</code> directory we can have a group of packages. And the packages can be &quot;optional packages&quot; or &quot;start packages.&quot;</p><p>Let&#39;s try to setup an example:</p><p>Usually our Neovim config directory is already included in the <code>packpath</code> list. This can be a valid path to create a group of packages.</p><p>Say we want to create a group called <code>vendor</code>. We should create a directory in one of these paths:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>~/.config/nvim/pack/vendor/         (Unix and OSX)</span></span>
<span class="line"><span>~/AppData/Local/nvim/pack/vendor/   (Windows)</span></span></code></pre></div><p>Is worth mention the name of the group can be anything. <code>vendor</code> is just a name I like.</p><p>Inside <code>vendor</code> we can have two types of packages: <code>opt</code> and <code>start</code>.</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>vendor</span></span>
<span class="line"><span>├── opt</span></span>
<span class="line"><span>│   └── package-one</span></span>
<span class="line"><span>└── start</span></span>
<span class="line"><span>    ├── package-two</span></span>
<span class="line"><span>    └── package-three</span></span></code></pre></div><p>Packages in <code>opt</code> are considered optional. Neovim will not use them until we execute the command <a href="https://neovim.io/doc/user/repeat.html#%3Apackadd" target="_blank" rel="noreferrer">:packadd</a>.</p><p>Packages in <code>start</code> will be loaded automatically during the startup process of Neovim.</p><p>Inside <code>opt</code> or <code>start</code> is where we actually put the plugins we want to use.</p><h2 id="git-clone-a-plugin" tabindex="-1">Git clone a plugin <a class="header-anchor" href="#git-clone-a-plugin" aria-label="Permalink to &quot;Git clone a plugin&quot;">​</a></h2><p>The only thing we need to install a plugin is <code>git</code> and knowing a valid path inside the <code>packpath</code>.</p><p>Let&#39;s say we are on a Linux system and we want to download <a href="https://github.com/echasnovski/mini.nvim" target="_blank" rel="noreferrer">mini.nvim</a>.</p><p>We can execute this command on a terminal.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clone</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://github.com/echasnovski/mini.nvim</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  ~/.config/nvim/pack/vendor/start/mini.nvim</span></span></code></pre></div><p>Is worth mention <code>mini.nvim</code> only supports Neovim v0.9 or greater. If you need to use it in Neovim v0.7, change to the commit <code>dd71253c</code>.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.config/nvim/pack/vendor/start/mini.nvim</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> switch</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --detach</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dd71253c8ab1569f7664034579345f3ae10efa81</span></span></code></pre></div><p>After we download a plugin we should generate the help tags, so the <code>:help</code> command can find the documentation of the plugin.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nvim</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --headless</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;helptags ALL&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;quit&#39;</span></span></code></pre></div><p>And that&#39;s it. Now we can use <code>mini.nvim</code> in our personal config.</p><p>Showing how to use <code>mini.nvim</code> is a story for another day. But we can check the plugin is available by trying to read the help page of one its modules. We can try for example reading the help page of <code>mini.pick</code>.</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:help mini.pick</span></span></code></pre></div>`,31)]))}const k=e(n,[["render",o]]);export{g as __pageData,k as default};
