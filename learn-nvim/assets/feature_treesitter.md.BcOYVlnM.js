import{_ as s,c as i,a2 as t,o as a}from"./chunks/framework.DXKzXLg0.js";const c=JSON.parse('{"title":"Treesitter","description":"","frontmatter":{"prev":{"text":"Features","link":"/feature/index"},"next":false},"headers":[],"relativePath":"feature/treesitter.md","filePath":"feature/treesitter.md"}'),n={name:"feature/treesitter.md"};function r(h,e,l,p,o,d){return a(),i("div",null,e[0]||(e[0]=[t(`<h1 id="treesitter" tabindex="-1">Treesitter <a class="header-anchor" href="#treesitter" aria-label="Permalink to &quot;Treesitter&quot;">​</a></h1><p>Neovim&#39;s <strong>treesitter support</strong> has been an experimental feature for a few years now. Even today, on the latest stable (<code>v0.12</code>) is marked as experimental. Since treesitter is a buzzword some people like to shout without any explanation here I&#39;ll try my best to shed a light on this topic.</p><h2 id="what-s-treesitter" tabindex="-1">What&#39;s treesitter? <a class="header-anchor" href="#what-s-treesitter" aria-label="Permalink to &quot;What&#39;s treesitter?&quot;">​</a></h2><p><a href="https://tree-sitter.github.io/tree-sitter/" target="_blank" rel="noreferrer">The official documentation</a> describes treesitter as an incremental parsing library. In other words, treesitter&#39;s main job is to read plain text and transform it into a data structure.</p><p><em>And how is that useful?</em></p><p>It&#39;s easier to extract information from structured data in comparison to plain text. This is the same thing &quot;compiled languages&quot; do. They process plain text and generate an <strong>Abstract Syntax Tree</strong> (AST). And all the fancy analysis a compiler can do is based on the AST.</p><p>To be clear, treesitter only reads plain text and generates an Abstract Syntax Tree. And it&#39;s Neovim&#39;s job to do something useful with this &quot;tree.&quot;</p><p>Treesitter&#39;s other job is to provide tools so the editor and plugin authors can extract data out of the AST.</p><h2 id="syntax-highlight" tabindex="-1">Syntax highlight <a class="header-anchor" href="#syntax-highlight" aria-label="Permalink to &quot;Syntax highlight&quot;">​</a></h2><p>Syntax highlight was one of the first areas where treesitter was used, is <strong>the</strong> feature that made treesitter famous in the Neovim community. Because in many cases treesitter&#39;s AST was more accurate than Vim&#39;s syntax files.</p><p>This new treesitter based highlight has the same purpose as syntax files. All it does is create &quot;highlight groups.&quot; And the colorscheme author is the one that needs to use these new highlight groups.</p><p>And this shows perfectly why treesitter is so hard to explain in simple words. Treesitter by itself is not a feature for casual users. It&#39;s the editor itself and the people that knows how to extend it (plugin authors) the ones creating the features that casual users see and interact with.</p><h2 id="other-use-cases" tabindex="-1">Other use cases <a class="header-anchor" href="#other-use-cases" aria-label="Permalink to &quot;Other use cases&quot;">​</a></h2><p>In Neovim&#39;s lua api we have access to a module called <a href="https://neovim.io/doc/user/treesitter.html#_lua-module:-vim.treesitter" target="_blank" rel="noreferrer">vim.treesitter</a> and this means any user with enough patience can start implementing features based on treesitter. This is what many plugins use internally. For example...</p><p><a href="https://github.com/nvim-mini/mini.nvim/blob/main/readmes/mini-snippets.md" target="_blank" rel="noreferrer">mini.snippets</a> (a module of <a href="https://github.com/echasnovski/mini.nvim" target="_blank" rel="noreferrer">mini.nvim</a>) can use treesitter to help provide context aware snippets. When setup correctly <code>mini.snippets</code> can use treesitter to figure out what is the language of the symbol under the cursor. Then it uses this information to search the user&#39;s personal snippet collection.</p><p><a href="https://github.com/nvim-treesitter/nvim-treesitter-context" target="_blank" rel="noreferrer">nvim-treesitter-context</a> is another interesting plugin. It uses treesitter to figure out where you are in the current function and decide if it should display the context at the top of the window.</p><p>Hopefully these examples showcase the value of Neovim&#39;s treesitter integration. It isn&#39;t just something for colorschemes, it&#39;s a tool that can be used to build cool features.</p><h2 id="language-support" tabindex="-1">Language support <a class="header-anchor" href="#language-support" aria-label="Permalink to &quot;Language support&quot;">​</a></h2><p>This is where things get interesting.</p><p>Treesitter doesn&#39;t have support for any language by default. Is not like the developers of the treesitter have to add support for a specific language. They decided to make it modular in a way. So there are three components to treesitter: parsers, queries and the library.</p><h3 id="treesitter-library" tabindex="-1">Treesitter library <a class="header-anchor" href="#treesitter-library" aria-label="Permalink to &quot;Treesitter library&quot;">​</a></h3><p>The &quot;library&quot; is the part of treesitter that is integrated into Neovim. Is the thing that process the content of a file.</p><h3 id="treesitter-parsers" tabindex="-1">Treesitter parsers <a class="header-anchor" href="#treesitter-parsers" aria-label="Permalink to &quot;Treesitter parsers&quot;">​</a></h3><p>Parsers are language specific. This is the component that knows how to read the code of a programming language and generates the AST.</p><p>I believe parsers are also &quot;libraries&quot; but these are like modules other people can implement. This is how you add support for a language, you install a treesitter parser for the language you want to use.</p><h3 id="treesitter-queries" tabindex="-1">Treesitter queries <a class="header-anchor" href="#treesitter-queries" aria-label="Permalink to &quot;Treesitter queries&quot;">​</a></h3><p>This is the mechanism we use to extract information out of the AST. Queries are written in a lisp-like language and it&#39;s basically how we search within the AST.</p><p>This is an example query for the json parser.</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>(object</span></span>
<span class="line"><span>  (pair</span></span>
<span class="line"><span>    key: (string) @some-thing))</span></span></code></pre></div><p>The entire AST treesitter creates can be represented in this syntax and a query is a pattern that can match one or more nodes in the file.</p><p>In some cases the query is the component that powers a specific feature. The syntax highlight for example. In this case Neovim will search the <a href="/learn-nvim/feature/global-plugin.html#the-runtimepath">runtimepath</a> any file that matches the pattern <code>queries/*/highlights.scm</code>. Where <code>*</code> is replaced by the name of a treesitter parser. And so <code>highlight.scm</code> will be the file that contains the queries that assigns the highlight groups.</p><h2 id="about-nvim-treesitter" tabindex="-1">About nvim-treesitter <a class="header-anchor" href="#about-nvim-treesitter" aria-label="Permalink to &quot;About nvim-treesitter&quot;">​</a></h2><p><a href="https://github.com/nvim-treesitter/nvim-treesitter" target="_blank" rel="noreferrer">nvim-treesitter</a> was probably the first plugin to ever implement features based on treesitter. It had multiple goals:</p><ul><li>Provide commands to install treesitter parsers</li><li>Provide queries for the supported parsers</li><li>Implement experimental features that may land in Neovim in the future</li></ul><p>The overall idea of this plugin was to make it easier to use treesitter within Neovim. Starting with the treesitter parsers, <code>nvim-treesitter</code> provides the command <code>:TSInstall</code> so we can add new treesitter parsers. Additionally, it provides queries for highlights, indents and folds.</p><p>On april 2026 the plugin was archived. The Neovim team <a href="https://github.com/neovim/neovim/issues/39006" target="_blank" rel="noreferrer">is planning something</a> to replace it, but we will have to wait before something concrete happens.</p><p>In its current state nvim-treesitter is only compatible with Neovim <strong>v0.12</strong>. And its usage is pretty simple:</p><p>First we install a parser using the command <code>:TSInstall</code>, for example.</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:TSInstall bash</span></span></code></pre></div><p>This will download the treesitter parser for bash, compile it and copy all the query files into Neovim&#39;s runtimepath.</p><p>Once everything is in place we procede to enable whatever features we have available. Right now it&#39;s just syntax highlight and code folding. For this we can use an autocommand.</p><div class="language-lua vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- neovim filetypes where you want to enable treesitter</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">local</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ts_filetypes </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sh&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">vim.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">api</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">nvim_create_autocmd</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;FileType&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  pattern </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ts_filetypes,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  callback</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    -- enable syntax highlight</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    vim.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">treesitter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    -- enable folds</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    vim.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">wo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">][</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">foldexpr</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;v:lua.vim.treesitter.foldexpr()&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    vim.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">wo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">][</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">foldmethod</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;expr&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  end</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p>The equivalent in vimscript would something like this:</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TreesitterStart</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  &quot; enable syntax highlight</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  lua </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vim.treesitter.start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  &quot; enable folds</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  setlocal</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> foldexpr</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">v:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lua.vim.treesitter.foldexpr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  setlocal</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> foldmethod</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">expr</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">endfunction</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">autocmd</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> FileType</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> sh</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> call</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TreesitterStart</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre></div><p>Notice how these two features we enabled come from <code>vim.treesitter</code>. This means they are built into Neovim. They can work as long as the parser and the query files are located in the runtimepath. If we were to delete nvim-treesitter but keep the parser and the queries, the features would still work.</p><h2 id="treesitter-without-plugins" tabindex="-1">Treesitter without plugins <a class="header-anchor" href="#treesitter-without-plugins" aria-label="Permalink to &quot;Treesitter without plugins&quot;">​</a></h2><p>Some people seem to think treesitter features were introduced in Neovim <strong>v0.12</strong>. That&#39;s just not true. <a href="https://neovim.io/doc/user/treesitter/#vim.treesitter.start()" target="_blank" rel="noreferrer">vim.treesitter.start()</a> and <a href="https://neovim.io/doc/user/treesitter/#vim.treesitter.foldexpr()" target="_blank" rel="noreferrer">vim.treesitter.foldexpr()</a> were introduced in Neovim <strong>v0.9</strong>. So a life without nvim-treesitter has been a possibility since april 2022. We just have to know how to install a parser.</p><p>To compile a parser we need <a href="https://github.com/tree-sitter/tree-sitter" target="_blank" rel="noreferrer">treesitter&#39;s CLI tool</a> and a C compiler available in our system.</p><p>Now let&#39;s assume we are on a linux system and we want to install the <a href="https://github.com/tree-sitter/tree-sitter-bash" target="_blank" rel="noreferrer">treesitter parser for bash</a>.</p><p>First thing we should do is download the source code for the parser. A good old <code>git clone</code> would do the trick.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clone</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://github.com/tree-sitter/tree-sitter-bash</span></span></code></pre></div><p>Now we navigate to the directory we just downloaded.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tree-sitter-bash</span></span></code></pre></div><p>And this is were we compile the parser using the CLI tool. In most cases this command is all we need:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tree-sitter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -o</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> parser.so</span></span></code></pre></div><p>Once the parser has been compiled we can move it to a directory in <a href="/learn-nvim/feature/global-plugin.html#the-runtimepath">Neovim&#39;s runtimepath</a>. Our Neovim config directory can be an option.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./parser.so</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.config/nvim/parser/bash.so</span></span></code></pre></div><p>Note that treesitter parsers must be located in a directory called <code>parser</code>. So, if you don&#39;t have one, create it. Here, the name of the file becomes the name of the parser.</p><p>Usually the name of the parser determines in which filetype is going to be used. But in this case <code>bash</code> is not a filetype name in Neovim. For bash we must &quot;register&quot; the filetype where we want this parser to be used. We add this somewhere in our Neovim configuration.</p><div class="language-lua vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">vim.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">treesitter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">language</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;bash&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sh&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p>Here we are saying we want to use the <code>bash</code> parser whenever we encounter the filetype <code>sh</code>.</p><p>Now we need the query files that will power the features we want to enable. Some parser do have query files included in the source code. Notice in the bash repository there is a directory called <a href="https://github.com/tree-sitter/tree-sitter-bash/tree/a06c2e4415e9bc0346c6b86d401879ffb44058f7/queries" target="_blank" rel="noreferrer">queries</a>. We can use that if we want.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./queries/highlights.scm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.config/nvim/queries/bash/highlights.scm</span></span></code></pre></div><p>Query files must be located in a directory called <code>queries</code>. And the specific queries for a parser must be in a sub-directory that has the same name as the parser.</p><p>Do note that query files that come with the parser source code are generic. They should work but there is no guarantee. Remember that queries are tied to the specific implementation of a feature. See for example <a href="https://github.com/tree-sitter/tree-sitter-bash/blob/a06c2e4415e9bc0346c6b86d401879ffb44058f7/queries/highlights.scm" target="_blank" rel="noreferrer">queries/highlights.scm</a> in the bash parser. And compare that to the queries provided by nvim-treesitter in <a href="https://github.com/nvim-treesitter/nvim-treesitter/blob/4916d6592ede8c07973490d9322f187e07dfefac/runtime/queries/bash/highlights.scm" target="_blank" rel="noreferrer">runtime/queries/bash/highlights.scm</a>. The one in nvim-treesitter has a lot more information and is aware of the convention Neovim uses for highlight groups. The one in the bash parser uses generic names and is smaller in size.</p><p>Anyway, now that we have the parser library compiled and the query file for highlights in place is time to enable the feature in Neovim. For this we just have to execute the function <code>vim.treesitter.start()</code> after the <code>FileType</code> event.</p><p>This is how we do it in lua.</p><div class="language-lua vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">vim.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">api</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">nvim_create_autocmd</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;FileType&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  pattern </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sh&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">},</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  callback</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    vim.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">treesitter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  end</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p>And this is the equivalent in vimscript.</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">autocmd</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> FileType</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> sh</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> lua </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vim.treesitter.start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre></div><p>If you are wondering why we only enable highlights and not code folding, that is because the bash parser does not have queries for code folding. We would have to copy the query file from nvim-treesitter (<a href="https://github.com/nvim-treesitter/nvim-treesitter/blob/main/runtime/queries/bash/folds.scm" target="_blank" rel="noreferrer">runtime/queries/bash/folds.scm</a>) if we want to make <code>vim.treesitter.foldexpr()</code> work.</p><p>And this is it. That&#39;s all you need to know to make treesitter work in Neovim.</p>`,72)]))}const g=s(n,[["render",r]]);export{c as __pageData,g as default};
