import{_ as t,c as i,a2 as a,o as s}from"./chunks/framework.DXKzXLg0.js";const u=JSON.parse('{"title":"Edit multiple files","description":"","frontmatter":{},"headers":[],"relativePath":"101/edit-multiple-files.md","filePath":"101/edit-multiple-files.md"}'),o={name:"101/edit-multiple-files.md"};function n(l,e,h,p,r,d){return s(),i("div",null,e[0]||(e[0]=[a(`<h1 id="edit-multiple-files" tabindex="-1">Edit multiple files <a class="header-anchor" href="#edit-multiple-files" aria-label="Permalink to &quot;Edit multiple files&quot;">​</a></h1><p>Here we will learn how to work with multiple files in Neovim.</p><p>I will focus on built-in features that can be used without third party plugins or any extra configuration. The goal here is to show what we can do out the box.</p><h2 id="buffers-windows-and-tabs" tabindex="-1">Buffers, windows and tabs <a class="header-anchor" href="#buffers-windows-and-tabs" aria-label="Permalink to &quot;Buffers, windows and tabs&quot;">​</a></h2><p>We have to talk about how Neovim works for a moment. Specifically, what happens when we open a file.</p><p>Let&#39;s say we open our terminal in type this command:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nvim</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test.lua</span></span></code></pre></div><p>To show the content of the file Neovim will create a <a href="https://neovim.io/doc/user/tabpage.html#tab-page-intro" target="_blank" rel="noreferrer">tab-page</a> with one <a href="https://neovim.io/doc/user/windows.html#window" target="_blank" rel="noreferrer">window</a>. This <code>window</code> will display the <a href="https://neovim.io/doc/user/usr_22.html#22.4" target="_blank" rel="noreferrer">buffer</a> that holds the content of <code>test.lua</code>.</p><p>So a <code>tab-page</code> is basically a container that can hold a collection of <code>windows</code>.</p><p>A <code>window</code> is the part of the UI where Neovim shows the content of a <code>buffer</code>.</p><p>And <code>buffer</code> is the term Neovim uses to describe the text it holds in memory.</p><p>We can tell Neovim to open files in tab-pages or split windows from the terminal.</p><p>If we want to open two files side by side we can execute this command.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nvim</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -O</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test.txt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> another.txt</span></span></code></pre></div><p>The <code>-O</code> flag tells Neovim to open each file in a vertical window. To open files in a horizontal split we use the <code>-o</code> flag.</p><p>To open each file in its own tab-page we use the <code>-p</code> flag.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nvim</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test.txt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> another.txt</span></span></code></pre></div><p>Is important to note we don&#39;t need split windows or tab-pages to manage multiple files. Having one tab-page with one window is the default behavior. This also works:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nvim</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test.txt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> another.txt</span></span></code></pre></div><p>Here Neovim will open both files but it will only show the first one. The rest of the files in the argument list will be hidden. We can still navigate between files even if we have just one window.</p><h2 id="edit-file" tabindex="-1">:edit file <a class="header-anchor" href="#edit-file" aria-label="Permalink to &quot;:edit file&quot;">​</a></h2><p>Once we are inside Neovim we can use the <a href="https://neovim.io/doc/user/editing.html#%3Aedit_f" target="_blank" rel="noreferrer">:edit</a> command to open other files. Like this.</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:edit test.txt</span></span></code></pre></div><p>If the file <code>test.txt</code> exists Neovim will show its content in the current window. Else, it will create an empty buffer with the name <code>test.txt</code>.</p><p>Relative paths are supported, so this is valid.</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:edit ./test.txt</span></span></code></pre></div><p>Pressing the <code>Tab</code> key in any part of the file path will trigger the completion menu.</p><p><code>#</code> is a special argument, it represents the <a href="https://neovim.io/doc/user/editing.html#alternate-file" target="_blank" rel="noreferrer">alternate file</a>. Which is another way to say &quot;the previous file we were just editing before we moved to the current one.&quot;</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:edit </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#</span></span></code></pre></div><p>Going back to the alternate file is so common there is a keymap for it: <a href="https://neovim.io/doc/user/editing.html#CTRL-6" target="_blank" rel="noreferrer">ctrl + 6</a>.</p><p>Now, if we execute <code>:edit</code> without arguments Neovim will reload the file in the current window. This is useful in case the file was changed by another program and we want Neovim to pick up the changes.</p><p>Since we might find ourselves using <code>:edit</code> a lot there is an alias for it in Neovim&#39;s command-line, <code>:e</code>. This also works.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">:e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test.txt</span></span></code></pre></div><h2 id="navigate-between-files" tabindex="-1">Navigate between files <a class="header-anchor" href="#navigate-between-files" aria-label="Permalink to &quot;Navigate between files&quot;">​</a></h2><p>Neovim keeps track of every file we open in something called <a href="https://neovim.io/doc/user/usr_22.html#_listing-buffers" target="_blank" rel="noreferrer">the buffer list</a>. The information on this list is what we use to navigate between files.</p><p>The <a href="https://neovim.io/doc/user/windows.html#%3Als" target="_blank" rel="noreferrer">:ls</a> command can show the buffer list. It would look something like this.</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>4 #h   &quot;/test/text&quot;	 line 9</span></span>
<span class="line"><span>6  h   &quot;another.lua&quot; line 10</span></span>
<span class="line"><span>7 %a + &quot;version.c&quot;   line 32</span></span></code></pre></div><p>In the first column we have the buffer number. In the second column there is some data that describe the state of the buffer. The third column is the name of the buffer. And then we have the cursor position.</p><p>To navigate to a file on this list we use the <a href="https://neovim.io/doc/user/windows.html#%3Abuffer" target="_blank" rel="noreferrer">:buffer</a> command.</p><p>Example time:</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:buffer </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#</span></span></code></pre></div><p>That command will take us to the <code>alternate file</code>. That would be the first buffer in our list, <code>/test/text</code>. And if we execute <code>:ls</code> again we&#39;ll get this.</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>4 %a   &quot;/test/text&quot;	 line 9</span></span>
<span class="line"><span>6  h   &quot;another.lua&quot; line 10</span></span>
<span class="line"><span>7 #h + &quot;version.c&quot;   line 32</span></span></code></pre></div><p>Notice <code>/test/text</code> gets the <code>%a</code> notation now. <code>%</code> means is the current file. The <code>a</code> stands for <strong>active</strong>, it means there is a window showing the content of the buffer. <code>h</code> means it&#39;s a hidden buffer. Hidden buffers are loaded into memory but there is no window showing the content. The <code>+</code> sign means the buffer has unsaved changes.</p><p>The beautiful thing about <code>:buffer</code> is it only deals with the files in the buffer list. We can provide the buffer number as an argument.</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:buffer </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">6</span></span></code></pre></div><p>This would take us to <code>another.lua</code>.</p><p>Tab completion in <code>:buffer</code> only autocompletes names on the list. For example, we can start typing our command like this.</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:buffer an</span></span></code></pre></div><p>And then we press <code>Tab</code>. That would expand the name to <code>another.lua</code>. And once we have the full name we press <code>Enter</code>.</p><p>To remove a buffer from the list we use <a href="https://neovim.io/doc/user/windows.html#%3Abdelete" target="_blank" rel="noreferrer">:bdelete</a>. This command will &quot;unload&quot; the buffer from memory.</p><p>To navigate between opened buffers without specifying their name we have these commands:</p><ul><li><p><a href="https://neovim.io/doc/user/windows.html#%3Abnext" target="_blank" rel="noreferrer">:bnext</a> and <a href="https://neovim.io/doc/user/windows.html#%3Abprevious" target="_blank" rel="noreferrer">:bprev</a> to move between buffers in the list.</p></li><li><p><a href="https://neovim.io/doc/user/windows.html#%3Abfirst" target="_blank" rel="noreferrer">:bfirst</a> and <a href="https://neovim.io/doc/user/windows.html#%3Ablast" target="_blank" rel="noreferrer">:blast</a> to move between both extremes of the list.</p></li><li><p><a href="https://neovim.io/doc/user/windows.html#%3Abmodified" target="_blank" rel="noreferrer">:bmodified</a> will take us to the next buffer with unsaved changes.</p></li></ul><h2 id="split-windows" tabindex="-1">Split windows <a class="header-anchor" href="#split-windows" aria-label="Permalink to &quot;Split windows&quot;">​</a></h2><p>The <a href="https://neovim.io/doc/user/windows.html#%3Asplit" target="_blank" rel="noreferrer">:split</a> command creates a new window in the current tab-page. This is similar to <code>:edit</code>, we use it to open a new file. But in this case Neovim will create an horizontal window.</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:split test.txt</span></span></code></pre></div><p>If we don&#39;t provide a file path Neovim will still open a new window showing the current buffer. So, we would have two windows showing the same file.</p><p><a href="https://neovim.io/doc/user/windows.html#%3Avsplit" target="_blank" rel="noreferrer">:vsplit</a> will create a new vertical window.</p><p>The command <a href="https://neovim.io/doc/user/windows.html#%3Aclose" target="_blank" rel="noreferrer">:close</a> will close the current window but it will keep the buffer loaded in memory. So it will remove the window but keep the file open.</p><p><a href="https://neovim.io/doc/user/editing.html#%3Aquit" target="_blank" rel="noreferrer">:quit</a> is like <code>:close</code>, we use it to close a window. But <code>:quit</code> will exit Neovim if we try to close the last window.</p><p>All these window commands can be triggered by keymaps:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ctrl-w + s -&gt; :split</span></span>
<span class="line"><span>ctrl-w + v -&gt; :vsplit</span></span>
<span class="line"><span>ctrl-w + c -&gt; :close</span></span>
<span class="line"><span>ctrl-w + q -&gt; :quit</span></span></code></pre></div><p>And speaking of keymaps, we also have a few to navigate between windows:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ctrl-w + h -&gt; moves the cursor to the window on the left</span></span>
<span class="line"><span>ctrl-w + j -&gt; moves the cursor to the window below</span></span>
<span class="line"><span>ctrl-w + k -&gt; moves the cursor to the window above</span></span>
<span class="line"><span>ctrl-w + l -&gt; moves the cursor to the window on the right</span></span></code></pre></div><h2 id="tab-page" tabindex="-1">Tab-page <a class="header-anchor" href="#tab-page" aria-label="Permalink to &quot;Tab-page&quot;">​</a></h2><p>A <code>tab-page</code> in Neovim is slightly different than a regular tab in other text editors. A <code>tab-page</code> is like a workspace that can show multiple files at a time.</p><p><a href="https://neovim.io/doc/user/tabpage.html#%3Atabedit" target="_blank" rel="noreferrer">:tabedit</a> will open a new tab-page. So if you want a workflow similar to other text editors you would open every new file with <code>:tabedit</code> instead of <code>:edit</code>.</p><p>Just like with buffers, Neovim keeps a list of tab-pages and provides a set of command manage it.</p><ul><li><p><a href="https://neovim.io/doc/user/tabpage.html#%3Atabs" target="_blank" rel="noreferrer">:tabs</a> shows the list of tab-pages.</p></li><li><p><a href="https://neovim.io/doc/user/tabpage.html#%3Atabnext" target="_blank" rel="noreferrer">:tabnext</a> go to the next tap-page on the list.</p></li><li><p><a href="https://neovim.io/doc/user/tabpage.html#%3Atabprevious" target="_blank" rel="noreferrer">:tabprev</a> go to the previous tab-page on the list.</p></li><li><p><a href="https://neovim.io/doc/user/tabpage.html#%3Atabclose" target="_blank" rel="noreferrer">:tabclose</a> will close all the windows in the current tab-page.</p></li><li><p><a href="https://neovim.io/doc/user/tabpage.html#%3Atabmove" target="_blank" rel="noreferrer">:tabmove</a> is used to re-order items in the list.</p></li></ul><p>The <code>:tabnext</code> command can take a number as an argument. That number would be the place on the tab-page list.</p><p>So imagine we have 10 files opened, each one in a tab-page. If we want to go to the third tab-page we execute this command.</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:tabnext </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span></span></code></pre></div><p>The short alias for <code>:tabnext</code> is <code>:tabn</code>. So we can write <code>:tabn 3</code> and pretend it means &quot;go to tab number 3.&quot;</p><p>In normal mode the keymap <code>gt</code> will trigger <code>:tabnext</code> and <code>gT</code> will trigger <code>:tabprev</code>.</p><p>There is something I haven&#39;t told you about keymaps, sometimes we can prefix them with a number. That&#39;s called a <strong>count</strong>. 99% of the time this count is used to repeat the keymap. <code>3w</code> would be the same as pressing <code>w</code> three times. Now, <code>gt</code> is part of the 1%. The implementation of <code>gt</code> uses the count as the tab-page number. So <code>3gt</code> would take us to the third tab-page.</p><p>Now, <code>:tabmove</code> is kind of funny. When we provide a number as an argument Neovim will move the tab-page <strong>next</strong> to the one we specify. Say we do this.</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:tabmove </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span></span></code></pre></div><p>This command would move the current tab-page to the <strong>third place</strong> on the list. Isn&#39;t that lovely?</p><h2 id="find-files" tabindex="-1">Find files <a class="header-anchor" href="#find-files" aria-label="Permalink to &quot;Find files&quot;">​</a></h2><p>There will be times when we don&#39;t know the full name of the file we want to open. In this case <code>:edit</code> is not so useful. If we want to search for a file we use the <a href="https://neovim.io/doc/user/editing.html#%3Afind" target="_blank" rel="noreferrer">:find</a> command.</p><p>The main feature of <code>:find</code> is actually the behavior of the tab completion. Not the execution of the command itself. If that makes any sense.</p><p>Let&#39;s say we want to search for a file, we know it has the word <code>config</code> somewhere in the path but we don&#39;t know where it is. We can go to command-line mode type this.</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:find config</span></span></code></pre></div><p>But we don&#39;t press <code>Enter</code> just yet. We press <code>Tab</code> to trigger the completion menu. After we select one of the items in the completion menu the argument in the command-line will expand. Once we have the full path we press <code>Enter</code>.</p><p>By default <code>:find</code> will look for files in the current working directory, the directory of the current file... and if you are in a &quot;unix system&quot; it would also look in <code>/usr/include/</code>.</p><p>We can configure the list of directories where <code>:find</code> looks for files using the <a href="https://neovim.io/doc/user/options.html#&#39;path&#39;" target="_blank" rel="noreferrer">path</a> option.</p><p>Some Vim users set the <code>path</code> like this.</p><div class="language-vim vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vim</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">set</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> path</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.,,**</span></span></code></pre></div><p><code>path</code> is a comma separated list. The first item is a dot, it means the directory of the current file. The second item is empty, it means the current working directory. The third item <code>**</code> is like a glob pattern, this means look inside every single directory in the current working directory. Think twice before using this in a directory that has millions of files.</p>`,89)]))}const m=t(o,[["render",n]]);export{u as __pageData,m as default};
